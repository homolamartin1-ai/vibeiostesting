# Appium — you build this here (Section 9)

This folder starts **empty on purpose**. In **Section 9** you build the Appium suite
(Python + pytest, Page Object Model) yourself, with the AI agent — using the
test-authoring skill and the prompts in
[`../prompts/section-09-appium.md`](../prompts/section-09-appium.md).

By the end of the section this folder holds your `conftest.py`, `pages/`, and `tests/`.

> Stuck? Your instructor builds this suite live during the course and shares the finished
> version as the class answer key — give it a genuine attempt yourself first.
