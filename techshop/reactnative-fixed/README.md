# TechShop iOS — React Native (Fixed)

The **clean** React Native build of TechShop. All 17 planted bugs are corrected. Used in
Section 13 for the regression / verification pass — your suite should go green here.

## Run it

```bash
npm install
npx expo start --ios
```

**Credentials:** demo@techshop.com / password123

Every fix is marked with a `FIXED (BUG-0xx)` comment next to the corrected code, so you can
diff this against `../reactnative-broken` to see exactly what changed.
